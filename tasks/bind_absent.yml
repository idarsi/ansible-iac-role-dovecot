---
- name: "Rechecking bind removal scope immediately before removal"
  ansible.builtin.include_tasks: validate_bind_absent.yml

- name: "Rechecking bind purge sources before removal"
  ansible.builtin.include_tasks: validate_bind_purge_sources.yml

- name: "Including shared bind mount absent tasks"
  ansible.builtin.include_tasks: shared/bind_absent.yml
